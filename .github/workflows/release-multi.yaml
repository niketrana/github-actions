name: Multi Job Release Pipeline

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Application
        run: |
          echo "Building release..."
          mkdir build
          echo "Release build for ${{ github.ref }}" > build/build.txt
          cat build/build.txt

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: build/

  package:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: build-files
          path: build/

      - name: Package Application
        run: |
          echo "Packaging release..."
          mkdir package
          cp build/build.txt package/package.txt
          cat package/package.txt

      - name: Upload Package Artifact
        uses: actions/upload-artifact@v4
        with:
          name: package-files
          path: package/

  release:
    runs-on: ubuntu-latest
    needs: package

    steps:
      - name: Download Package Artifact
        uses: actions/download-artifact@v4
        with:
          name: package-files
          path: package/

      - name: Release Step
        run: |
          echo "Release completed"
          cat package/package.txt

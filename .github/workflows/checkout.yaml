name: Checkout Lab

on:
  push:
    branches:
      - checkout-labe
#    paths:
#      - '.github/workflows/checkout.yaml'

jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Files
        run: ls -la

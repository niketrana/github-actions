name: Docker Login Simple

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  docker-login:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Login
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Login Success
        run: echo "Docker login successful"

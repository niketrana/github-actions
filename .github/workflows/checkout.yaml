name: Checkout Lab

on:
  push:
    branches:
#      - main
      - checkout-lab
#    paths:
#      - '.github/workflows/checkout.yaml'

jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Files
        run: ls -l

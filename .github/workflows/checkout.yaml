name: Checkout Lab

on:
#  push 
  workflow_dispatch:

jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Files
        run: ls -la

      - name: Show Current Directory
        run: pwd
